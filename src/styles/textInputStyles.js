// textInputStyles.js
import { StyleSheet } from 'react-native';

const textInputStyles = StyleSheet.create({
  inputContainer: {
    borderBottomWidth: 1,
    borderBottomColor: 'gray',
    marginBottom: 10,
  },
  // Các kiểu dáng khác cho TextInput có thể được thêm vào đây
});

export default textInputStyles;
